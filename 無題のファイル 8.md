---
tags: ""
datetimeCreate: 2026-03-28 14:42
---
**・オファー内容の前提となる採用等級および期待役割  
・入社後に想定される案件やアサインの方向性  
・CFO&EV領域の中で、どのようなテーマやミッションを担う想定か  
・評価および昇格の考え方、早期に期待される成果イメージ  
・Finance / CFO領域におけるAI活用テーマとの接点や機会  

・賞与の標準的な支給水準と評価による変動幅

・働き方の実態(実際の出社頻度、出張、常駐、関西所属でも東京案件が多いか。)**





```dataviewjs
dv.header(3, "関連ノート");
var maxLoop = Math.min(dv.current().file.tags.length, 3);
for(let i=0;i<maxLoop;i++){
dv.span(dv.current().file.tags[i]);
dv.list(dv.pages(dv.current().file.tags[i]).sort(f=>f.file.mtime.ts,"desc").limit(15).file.link);
}

for (let outgo of dv.pages('outgoing([[' + dv.current().file.name + ']])')) {
    dv.header(4, outgo.file.name);
    dv.list(outgo.file.inlinks.sort());
}
```